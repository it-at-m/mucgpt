version: 1
filters:
  correlation_id:
    (): asgi_correlation_id.CorrelationIdFilter
    uuid_length: 32
    default_value: '-'
formatters:
  simple:
    format: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
  http:
    format: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
    datefmt: '%Y-%m-%d %H:%M:%S'
  json:
    class: core.logtools.JsonFormatter
handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
    formatter: json
    filters: [correlation_id]
    stream: ext://sys.stdout
  httpx:
    class: logging.StreamHandler
    formatter: json
    filters: [correlation_id]
    stream: ext://sys.stderr
loggers:
  uvicorn.error:
    level: ${LOG_LEVEL_UVICORN_ERROR:-ERROR}
    handlers: [console]
    propagate: no
  uvicorn.access:
    level: ${LOG_LEVEL_UVICORN_ACCESS:-INFO}
    handlers: [console]
    propagate: no
  dev:
    level: ${LOG_LEVEL_DEV:-ERROR}
    handlers: [console]
    propagate: no
  httpx:
    level: ${LOG_LEVEL_HTTPX:-ERROR}
    handlers: [httpx]
    propagate: no
  httpcore:
    level: ${LOG_LEVEL_HTTPCORE:-ERROR}
    handlers: [httpx]
    propagate: no
  users_router:
    level: ${LOG_LEVEL_USERS_ROUTER:-ERROR}
    handlers: [console]
    propagate: no
  system_router:
    level: ${LOG_LEVEL_SYSTEM_ROUTER:-ERROR}
    handlers: [console]
    propagate: no
  assistants_router:
    level: ${LOG_LEVEL_ASSISTANTS_ROUTER:-ERROR}
    handlers: [console]
    propagate: no
  assistant_repo:
    level: ${LOG_LEVEL_ASSISTANT_REPO:-ERROR}
    handlers: [console]
    propagate: no
  mucgpt-assistant-service:
    level: ${LOG_LEVEL_MUCGPT_ASSISTANT_SERVICE:-ERROR}
    handlers: [console]
    propagate: no
  auth:
    level: ${LOG_LEVEL_AUTH:-ERROR}
    handlers: [console]
    propagate: no
root:
  level: ${LOG_LEVEL_ROOT:-ERROR}
  handlers: [console]
