# 015 - Deferred: Skills Middleware Adoption

## Status

**Not scheduled.** No concrete skill-library requirement yet.

## Goal

Record that `SkillsMiddleware` (load `SKILL.md`-style docs on demand) is
available natively once this migration lands, as an alternative to growing
`agent/prompt_pool/*.md` as an always-on system prompt for every request.

## Implementation (when picked up)

Scope against a real requirement — e.g. department-specific procedural
knowledge (citation conventions, document templates) that shouldn't be
always-on for every assistant.

## Acceptance Criteria

Not applicable until scoped against a real requirement.

## Reviewer Notes

Recorded so nobody has to rediscover that this exists once the harness is
in place — this is not an active task.
