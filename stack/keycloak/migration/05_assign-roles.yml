id: assign roles to users
author: Default
realm: local_realm
changes:
  - assignRole:
      user: demo-user
      role: mucgpt-user
      clientId: mucgpt
  - assignRole:
      user: demo-team-a-user
      role: mucgpt-user
      clientId: mucgpt
  - assignRole:
      user: demo-team-b-user
      role: mucgpt-user
      clientId: mucgpt
  # demo-no-access-user intentionally has no role assigned
