version: 1
filters:
  correlation_id:
    (): asgi_correlation_id.CorrelationIdFilter
    uuid_length: 32
    default_value: '-'
formatters:
  simple:
    format: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
  http:
    format: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
    datefmt: '%Y-%m-%d %H:%M:%S'
  json:
    class: core.logtools.JsonFormatter
handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
    formatter: json
    filters: [correlation_id]
    stream: ext://sys.stdout
  httpx:
    class: logging.StreamHandler
    formatter: json
    filters: [correlation_id]
    stream: ext://sys.stderr
loggers:
  uvicorn.error:
    level: DEBUG
    handlers: [console]
    propagate: no
  uvicorn.access:
    level: DEBUG
    handlers: [console]
    propagate: no
  dev:
    level: DEBUG
    handlers: [console]
    propagate: no
  mucgpt-core:
    level: DEBUG
    handlers: [console]
    propagate: no
  mucgpt-core-agent:
    level: DEBUG
    handlers: [console]
    propagate: no
  mucgpt-core-simply:
    level: DEBUG
    handlers: [console]
    propagate: no
  mucgpt-core-summarize:
    level: DEBUG
    handlers: [console]
    propagate: no
  mucgpt-core-brainstorm:
    level: DEBUG
    handlers: [console]
    propagate: no
  httpx:
    level: INFO
    handlers: [httpx]
    propagate: no
  httpcore:
    level: INFO
    handlers: [httpx]
    propagate: no
root:
  level: DEBUG
  handlers: [console]
