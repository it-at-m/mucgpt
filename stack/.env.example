# ============================================================================
# MUCGPT Docker Compose Environment Configuration
# ============================================================================
# This file contains infrastructure-related settings (proxies, SSL).
#
# Application-specific settings are in YAML config files:
#   - core.config.yaml       → core-service (models, Langfuse, MCP, Redis)
#   - assistant.config.yaml  → assistant-service & migrations (DB, Redis, LDAP)
#
# To get started:
#   1. cp .env.example .env
#   2. cp core.config.yaml.example core.config.yaml
#   3. cp assistant.config.yaml.example assistant.config.yaml
#   4. Edit the YAML files with your settings
#
# Environment variables can override any YAML setting using the nested
# delimiter __ (double underscore). Examples:
#   MUCGPT_CORE_REDIS__HOST=my-redis
#   MUCGPT_ASSISTANT_DB__PASSWORD=secret
# ============================================================================

# SSL Certificate Configuration
# Set SSL_CERT_FILE to empty string to disable the environment variable
# Set CA_BUNDLE_PATH to empty string to disable the volume mount
SSL_CERT_FILE=
CA_BUNDLE_PATH=
SSL_MOUNT_PATH=
# other
GIT_PYTHON_REFRESH=quiet
