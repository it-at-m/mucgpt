# ============================================================================
# MUCGPT Docker Compose Environment Configuration
# ============================================================================
# This file contains infrastructure-related settings for the gateway and services.
# Application-specific settings (models, database, etc.) are now in YAML config files:
#   - core-service-config.yaml
#   - assistant-service-config.yaml
#   - shared-config.yaml
#
# To use this file:
# 1. Copy this file to .env: cp .env.example .env
# 2. Edit the YAML config files to configure your services
# 3. Uncomment and configure the settings below as needed
# ============================================================================

# SSO Configuration (Keycloak) - Required for Gateway
MUCGPT_SSO_URL=http://keycloak:8100
MUCGPT_SSO_REALM=local_realm
MUCGPT_SSO_CLIENTID=mucgpt
MUCGPT_SSO_CLIENT_SECRET=client_secret
MUCGPT_SSO_SCOPE=profile,openid,LHM,LHM_Extended

# SSL Certificate Configuration
# Set SSL_CERT_FILE to empty string to disable the environment variable
# Set CA_BUNDLE_PATH to empty string to disable the volume mount
SSL_CERT_FILE=
CA_BUNDLE_PATH=
SSL_MOUNT_PATH=
# other
GIT_PYTHON_REFRESH=quiet
